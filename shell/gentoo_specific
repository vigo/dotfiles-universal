if [[ $- != *i* ]] ; then
    # Shell is non-interactive.  Be done now!
    return
fi

# Put your stuff here...
alias ls='ls -h --color --group-directories-first'
alias ll='ls -alF'
alias la='ls -A'
alias l='ls -CF'
